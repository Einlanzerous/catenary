name: catenary_wire
description: >-
  Generated Catenary wire protocol types and JSON codecs for the Flutter client.
  Everything under lib/src/generated.dart comes from
  ../schema/catenary.wire.v1.schema.json — see IDEA-27 (R4).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
